## Agent skills

### Issue tracker

Local Markdown issues under `.scratch/<feature>/issues/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout with `CONTEXT.md` at the repository root and ADRs under `docs/adr/`. See `docs/agents/domain.md`.
