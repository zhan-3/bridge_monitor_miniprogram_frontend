# 桥梁监测小程序

本上下文描述用户绑定并查看监测设备时使用的业务语言。

## Language

**已绑定设备集**：
用户已经绑定的全部设备，以及当前选中的设备。每台设备包含自己的 SN、设备别名和设备访问凭证。
_Avoid_：设备会话、设备 Token 列表

**设备别名**：
用户为方便自己识别设备而设置的名称。同一台设备可以因用户不同而拥有不同别名。
_Avoid_：设备全局名称、共享名称

**安装位置**：
设备当前安装地点的共享信息。关联用户看到同一位置，也可以用新的安装位置覆盖它。
_Avoid_：报警位置、用户私人位置

**报警位置**：
一次报警事件发生时记录的位置，只属于该报警事件，不能代替设备安装位置。
_Avoid_：安装位置、设备当前位置

**登录凭证**：
证明当前用户身份并授权用户级操作的凭证，包括绑定新设备；它不对应任何一台设备。
_Avoid_：初始 Token、基础 Token、用户 Token、通用 Token

**登录状态**：
用户持有且已确认有效的登录凭证。它不表示用户已经绑定手机号或设备。
_Avoid_：手机号状态、设备状态、可用状态

**手机号资格**：
用户已经通过短信验证并绑定手机号，因此可以使用依赖手机号的报警功能。它与登录状态分别判断。
_Avoid_：登录状态、登录成功

**设备访问凭证**：
授权当前用户访问一台指定已绑定设备的凭证；每份凭证只对应一个设备 SN。
_Avoid_：完整 Token、设备 Token、当前 Token、通用 Token

**报警事件**：
设备在某一时刻主动上报的一次事实记录。它可以包含位置和录音，但不表示设备之后持续处于报警状态。
_Avoid_：实时报警状态、设备异常状态

**处理状态**：
人员是否已经确认处理某个报警事件。处理状态由收到该事件的用户共享；“已处理”不表示设备已经恢复正常。
_Avoid_：设备恢复状态、已查看
