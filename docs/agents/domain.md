# Domain documentation

This repository uses a **single-context** layout.

- Domain glossary: `CONTEXT.md` at the repository root, when present.
- Architecture decisions: `docs/adr/`, when present.

Engineering skills should read these documents before proposing or changing domain terminology or architecture. Neither document exists yet; create them lazily when the domain model or an architectural decision needs to be recorded.
